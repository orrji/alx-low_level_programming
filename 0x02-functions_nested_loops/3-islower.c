#include "main.h"

/**
 * _islower - Checks if c is lowercase
 *
 * Return: 1 if lowercase and 0 if otherwise
 */

int _islower(int c)
{
	int c;

	if (c >= 'a' && c<= 'z')
	{
		_putchar(1);
		else (0);
	}
	return (0);
}
